# Install exFat Format Drive

The official packages `exfat-utils` and `exfat-fuse` does the trick as it turns out.

```bash
$ sudo apt-get update
$ sudo apt-get install exfat-utils exfat-fuse
$ sudo reboot
```
